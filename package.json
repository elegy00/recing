{
  "name": "recing",
  "private": true,
  "packageManager": "pnpm@11.8.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@types/node": "^25.9.2",
    "@types/pg": "^8.21.0",
    "typescript": "^6.0.3"
  }
}
