app = "recing"
primary_region = "ord"

[build]
dockerfile = "Dockerfile.web"

[[services]]
internal_port = 3000
protocol = "tcp"

[services.concurrency]
type = "connections"
hard_limit = 100
soft_limit = 50

[[vm]]
memory = "256mb"
cpu_kind = "shared"
cpus = 1
